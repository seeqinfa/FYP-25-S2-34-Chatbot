# rasa/Dockerfile.rasa
FROM rasa/rasa:3.6.21-full # Changed from 3.x.x-full-python3.9 to 3.6.21-full

WORKDIR /app

COPY . /app

RUN pip install --no-cache-dir -r requirements.txt

RUN rasa train

EXPOSE 5005

CMD ["rasa", "run", "--enable-api", "--cors", "*", "--port", "5005"]
