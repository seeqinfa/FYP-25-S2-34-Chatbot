[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqli", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "nginx"]

[phases.build]
cmds = ["mkdir -p /var/log/nginx", "mkdir -p /var/lib/nginx"]

[phases.start]
cmd = "nginx -g 'daemon off;'"

[variables]
PORT = "8080"